diff --git a/tank_realms_v25_standalone (1).html b/tank_realms_v25_standalone (1).html
index 51ac97c..fc23828 100644
--- a/tank_realms_v25_standalone (1).html	
+++ b/tank_realms_v25_standalone (1).html	
@@ -3087,7 +3087,7 @@
         }
         // v10: difficulty — v26: fire rate also ramps with the run level (capped at +125%)
         const enemyFireRoll = (base, dt) => Math.random() < base * dt * 60 * (state.diffMult.fire || 1)
-            * (1 + Math.min(1.25, Math.max(0, (state.level || 1) - 1) * 0.03));
+            * (1 + Math.min(0.55, Math.max(0, (state.level || 1) - 1) * 0.015)); // v27: gentler ramp
         function shoot(source) {
             if (source.isPlayer) SFX.shoot(); else SFX.enemyShoot(); // v23
             // Recoil animation with smooth return
@@ -3477,9 +3477,9 @@
         function enemyLevelScale() {
             const L = state.level || 1;
             return {
-                hp:  1 + Math.max(0, L - 1) * 0.09  + Math.max(0, L - 10) * 0.05 + Math.max(0, L - 20) * 0.06,
-                dmg: 1 + Math.max(0, L - 1) * 0.055 + Math.max(0, L - 12) * 0.045 + Math.max(0, L - 22) * 0.05,
-                spd: 1 + Math.min(0.5, Math.max(0, L - 1) * 0.014),
+                hp:  1 + Math.max(0, L - 1) * 0.055 + Math.max(0, L - 10) * 0.03 + Math.max(0, L - 20) * 0.04,
+                dmg: 1 + Math.max(0, L - 1) * 0.032 + Math.max(0, L - 12) * 0.028 + Math.max(0, L - 22) * 0.03,
+                spd: 1 + Math.min(0.35, Math.max(0, L - 1) * 0.010),
                 pts: 1 + Math.max(0, L - 1) * 0.05  + Math.max(0, L - 12) * 0.03
             };
         }
@@ -3493,12 +3493,11 @@
             if (level >= 6) types.push('bomber');   // v5
             if (level >= 8) types.push('phantom');  // v5
             if (level >= 10) types.push('gunner');  // v5
-            // v26: extra pushes weight the roster toward the newer threats late on
+            // v27: the newer threats appear at single weight — the double/triple
+            // weighting made late waves 44% hard types, which walled the run at ~lv15
             if (level >= 5) types.push('skirmisher');
-            if (level >= 6) types.push('skirmisher');
             if (level >= 10) types.push('juggernaut');
-            if (level >= 12) { types.push('wasp'); types.push('wasp'); types.push('wasp'); }
-            if (level >= 13) types.push('juggernaut');
+            if (level >= 12) types.push('wasp');
             return types[Math.floor(Math.random() * types.length)];
         }
 
@@ -3541,7 +3540,7 @@
             const bx = (player ? player.mesh.position.x : 0) + Math.cos(a) * 45;
             const bz = (player ? player.mesh.position.z : 0) + Math.sin(a) * 45;
             const boss = new Tank(ENEMY_TYPES[kind.type].color, false, kind.type);
-            boss.hp = boss.maxHp = Math.round(boss.maxHp * (1 + state.level * 0.045 + Math.max(0, state.level - 15) * 0.025)); // v26: steeper late scaling
+            boss.hp = boss.maxHp = Math.round(boss.maxHp * (1 + state.level * 0.032 + Math.max(0, state.level - 15) * 0.015)); // v27: softer late scaling
             boss.damageMult = enemyLevelScale().dmg; // v26
             boss.isBoss = true;
             boss.attackInterval = kind.interval;
@@ -4171,7 +4170,7 @@
             enemy.damageMult = sc.dmg;
             enemy.speedMult = sc.spd;
             enemy.pointValue = Math.round((ENEMY_TYPES[type].points || 100) * sc.pts);
-            if (type === 'juggernaut') enemy.armorFlat = 5 + state.level * 0.25; // v26: flat plating
+            if (type === 'juggernaut') enemy.armorFlat = 3 + state.level * 0.15; // v27: lighter plating
             enemy.mesh.position.set(x, 0, z);
             enemies.push(enemy);
             return enemy;
@@ -4717,13 +4716,14 @@
                 spawnBoss();
             }
 
-            // Spawner (v10: mode-aware — v26: faster cadence and a higher ceiling)
-            const floor = (state.mode === 'casual' && state.level > 20) ? 0.8 : 1.0;
-            const spawnRate = Math.max(floor, 3.5 - state.level * 0.22);
+            // Spawner (v10: mode-aware — v27: eased cadence and ceiling; 19-20 simultaneous
+            // enemies at lv15 was the main reason the run stalled there)
+            const floor = (state.mode === 'casual' && state.level > 20) ? 1.1 : 1.3;
+            const spawnRate = Math.max(floor, 3.5 - state.level * 0.16);
             let maxEnemies = state.mode === 'levels'
-                ? state.levelsCfg.density + Math.floor(state.level / 5)
-                : Math.min(20, 4 + state.level);
-            if (state.mode === 'casual' && state.level > 20) maxEnemies = Math.min(26, 17 + Math.floor((state.level - 20) / 3));
+                ? state.levelsCfg.density + Math.floor(state.level / 8)
+                : Math.min(15, 3 + state.level);
+            if (state.mode === 'casual' && state.level > 20) maxEnemies = Math.min(19, 14 + Math.floor((state.level - 20) / 4));
             if (clock.getElapsedTime() - state.lastSpawnTime > spawnRate) {
                 if (enemies.filter(e => !e.isDead).length < maxEnemies) {
                     spawnEnemy();
@@ -4736,7 +4736,7 @@
             if (state.surgeNextAt === undefined) state.surgeNextAt = (state.runTime || 0) + 50;
             if (!state.surgeActive && state.runTime >= state.surgeNextAt) {
                 state.surgeActive = true;
-                state.surgeEndsAt = state.runTime + 20;
+                state.surgeEndsAt = state.runTime + 15; // v27: shorter window
                 state.surgeLastSpawn = 0;
                 showBossBanner('⚠ ENEMY SURGE — HOLD THE LINE! ⚠');
                 SFX.bossAlarm(); SFX.vibrate([60, 50, 60, 50, 60]);
@@ -4748,10 +4748,10 @@
                     state.coins = (state.coins || 0) + bounty;
                     state.runCoins = (state.runCoins || 0) + bounty;
                     showBossBanner('⚔ Surge held! +' + bounty + ' 💰');
-                    state.surgeNextAt = state.runTime + 80 + Math.random() * 30;
+                    state.surgeNextAt = state.runTime + 95 + Math.random() * 35; // v27: more breathing room
                     try { saveGame(); } catch (e) {}
-                } else if (state.runTime - (state.surgeLastSpawn || 0) > 1.1) {
-                    if (enemies.filter(e => !e.isDead).length < maxEnemies + 6) {
+                } else if (state.runTime - (state.surgeLastSpawn || 0) > 1.5) { // v27: slower cadence
+                    if (enemies.filter(e => !e.isDead).length < maxEnemies + 3) { // v27: lower ceiling
                         spawnEnemy();
                         state.surgeLastSpawn = state.runTime;
                     }
