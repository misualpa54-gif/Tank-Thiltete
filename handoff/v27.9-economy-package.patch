diff --git a/tank_realms_latest.html b/tank_realms_latest.html
index bf7c9d2..ede1341 100644
--- a/tank_realms_latest.html
+++ b/tank_realms_latest.html
@@ -537,6 +537,13 @@
             opacity: 1;
         }
 
+        #revive-offer, #market-dialog {
+            position: fixed; inset: 0; z-index: 60;
+            display: flex; align-items: center; justify-content: center;
+            background: rgba(5, 8, 15, 0.72);
+        }
+        #revive-offer.hidden, #market-dialog.hidden { display: none; }
+
         #upgrade-notification {
             position: fixed;
             /* v27.5 (owner): smaller, quicker, tucked under the level + HP column */
@@ -921,6 +928,25 @@
         </div>
     </div>
 
+    <div id="revive-offer" class="hidden">
+        <div class="settings-panel" style="width: min(92vw, 380px);">
+            <div class="settings-title" style="font-size:1.3rem;">💀 Field Revive</div>
+            <div class="settings-subtitle">Death knocked — pay to knock back?</div>
+            <div style="font-size:16px; color:#ffd479; font-weight:700; margin:12px 0;">Revive at 50% HP for 💰 <span id="revive-cost">300</span></div>
+            <div style="display:flex; gap:10px; margin-top:10px;">
+                <button class="btn btn-restart" id="btn-revive-yes" style="margin:0; flex:1; padding:12px 0; font-size:14px;">✨ Revive</button>
+                <button class="hud-icon-btn wide" id="btn-revive-no" style="flex:1;">Give Up</button>
+            </div>
+        </div>
+    </div>
+    <div id="market-dialog" class="hidden">
+        <div class="settings-panel" style="width: min(92vw, 400px);">
+            <div class="settings-title" style="font-size:1.3rem;">🛒 Black Market</div>
+            <div class="settings-subtitle">Your coins: 💰 <span id="market-coins">0</span></div>
+            <div id="market-items" style="display:flex; flex-direction:column; gap:8px; margin-top:10px;"></div>
+            <button class="hud-icon-btn wide" id="btn-market-leave" style="margin-top:12px; width:100%;">Leave</button>
+        </div>
+    </div>
     <div id="save-dialog" class="hidden">
         <div class="settings-panel" style="width: min(92vw, 380px);">
             <div class="settings-title" style="font-size:1.3rem;">💾 Save run</div>
@@ -1318,7 +1344,8 @@
             invulnUntil: 0,              // v4 Second Wind
             bossPending: false, bossActive: null, bossCount: 0, // v6(C) boss gate
             activeSaveName: null, // v27.6: set when a run is loaded from a named save (overwrite target)
-            supplyNextAt: 0, rapidFireUntil: 0 // v27.6: supply drops + rapid-fire reward
+            supplyNextAt: 0, rapidFireUntil: 0, // v27.6: supply drops + rapid-fire reward
+            revivesUsed: 0, reviveOfferShown: false, damageBoostUntil: 0 // v27.9: Field Revive + market surge
         };
 
         let audioCtx = null;
@@ -3022,7 +3049,7 @@
                 });
 
                 if (this.hp <= 0 && !this.isDead) {
-                    if (this.isPlayer && state.reviveAvailable) { // v4+v10: Second Wind
+                    if (this.isPlayer && state.reviveAvailable) { // v4+v10: Second Wind — owned perk takes priority over the paid offer
                         SFX.revive(); SFX.vibrate([40, 60, 40]); // v23
                         state.reviveAvailable = false;
                         this.hp = Math.ceil(this.maxHp * (0.5 + 0.25 * (((state.meta || {}).revive || 1) - 1)));
@@ -3030,6 +3057,9 @@
                         createHealEffect(this.mesh.position);
                         showUpgradeNotification('✨ SECOND WIND — revived at 50% HP');
                         updateHUD();
+                    } else if (this.isPlayer && state.mode === 'casual' && (state.coins || 0) >= fieldReviveCost() && !state.reviveOfferShown) { // v27.9 (owner): Field Revive offer
+                        this.hp = 1; // hold — the offer decides
+                        openReviveOffer();
                     } else {
                         this.die();
                     }
@@ -3192,6 +3222,7 @@
             const damage = source.isPlayer
                 ? CONFIG.baseDamage * (state.playerStats.damage / 100)
                     * ((state.runTime || 0) < (state.overchargeUntil || 0) ? 1.3 : 1) // v26: Overcharge
+                    * ((state.runTime || 0) < (state.damageBoostUntil || 0) ? 1.2 : 1) // v27.9: Black Market damage surge
                 : (ENEMY_TYPES[source.type]?.damage || 12);
 
             for (let i = 0; i < shotCount; i++) {
@@ -3623,6 +3654,43 @@
             updateBossBar();
         }
 
+        // v27.9 (owner): FIELD REVIVE — when death would end a casual run and you can
+        // afford it, a one-tap offer revives you at 50% HP. Cost quadruples per use in
+        // the same run (300 -> 1,200 -> 4,800...), a recurring coin sink that scales
+        // with desperation.
+        function fieldReviveCost() { return 300 * Math.pow(4, state.revivesUsed || 0); }
+        function openReviveOffer() {
+            state.reviveOfferShown = true;
+            state.gamePhase = 'paused'; // freeze the battlefield behind the offer
+            document.getElementById('revive-cost').textContent = fieldReviveCost().toLocaleString();
+            document.getElementById('revive-offer').classList.remove('hidden');
+            SFX.bossAlarm();
+        }
+        function closeReviveOffer() { document.getElementById('revive-offer').classList.add('hidden'); }
+        function acceptRevive() {
+            const cost = fieldReviveCost();
+            if ((state.coins || 0) < cost) return;
+            state.coins -= cost;
+            state.revivesUsed = (state.revivesUsed || 0) + 1;
+            state.reviveOfferShown = false;
+            closeReviveOffer();
+            player.hp = Math.ceil(player.maxHp * 0.5);
+            state.invulnUntil = clock.getElapsedTime() + 2;
+            state.gamePhase = 'playing';
+            refreshCoinsHUD();
+            SFX.revive(); SFX.vibrate([40, 60, 40]);
+            createHealEffect(player.mesh.position);
+            showBossBanner('✨ FIELD REVIVE — back in the fight!');
+            updateHUD();
+            try { saveGame(); } catch (e) {}
+        }
+        function declineRevive() {
+            state.reviveOfferShown = false;
+            closeReviveOffer();
+            player.die();
+            endGame();
+        }
+
         function spawnBoss() {
             if (!state.isPlaying || !player) return;
             const kind = BOSS_KINDS[(state.bossCount || 0) % BOSS_KINDS.length];
@@ -3670,6 +3738,9 @@
             // v26: endless coin sinks — these never max out
             { id:'dmg_inf', icon:'🔧', name:'Master Gunsmith',   desc:'+1% base damage (per level, UNLIMITED)',   base:400, growth:1.18, max:Infinity },
             { id:'hp_inf',  icon:'🧱', name:'Reinforced Alloy',  desc:'+2 starting Max HP (per level, UNLIMITED)', base:350, growth:1.16, max:Infinity },
+            // v27.9 (owner): steeper unlimited sinks so coins always have a home
+            { id:'rate_inf', icon:'🔬', name:'Precision Optics',  desc:'+1% fire rate (per level, UNLIMITED)',      base:600, growth:1.35, max:Infinity },
+            { id:'hp5_inf',  icon:'🚪', name:'Blast Door',        desc:'+5 starting Max HP (per level, UNLIMITED)', base:300, growth:1.30, max:Infinity },
         ];
         const SKINS = [
             { id:'amber',   name:'Amber Strike',  color:0xf59e0b, cost:0 },
@@ -4011,6 +4082,8 @@
             missiles.forEach(m => scene.remove(m.group)); missiles = []; // v24
             particles.forEach(p => { scene.remove(p.mesh); disposeObject3D(p.mesh); }); particles = [];
             removeSupplyDrop(); // v27.6
+            state.reviveOfferShown = false; // v27.9
+            closeReviveOffer(); closeBlackMarket();
             document.querySelectorAll('.score-popup').forEach(p => p.remove());
             setScreenVisibility('pause-screen', false);
             setScreenVisibility('game-over-screen', false);
@@ -4086,6 +4159,8 @@
             state.playerStats.regen += (meta.regen || 0) * 1; // v10: Repair Kit
             state.playerStats.damage += (meta.dmg_inf || 0) * 1; // v26: Master Gunsmith (unlimited)
             state.playerStats.maxHp += (meta.hp_inf || 0) * 2;   // v26: Reinforced Alloy (unlimited)
+            state.playerStats.fireRate += (meta.rate_inf || 0) * 1; // v27.9: Precision Optics (unlimited)
+            state.playerStats.maxHp += (meta.hp5_inf || 0) * 5;     // v27.9: Blast Door (unlimited)
             state.reviveAvailable = (meta.revive || 0) > 0;
             // v26: surge scheduling on the pause-safe run clock
             state.surgeActive = false;
@@ -4093,6 +4168,8 @@
             state.rapidFireUntil = 0;
             state.invulnUntil = 0; // v27.8 (audit): Second Wind's 2s invulnerability leaked into the next run
             state.speedBoostUntil = 0;
+            state.revivesUsed = 0; state.reviveOfferShown = false; state.damageBoostUntil = 0; // v27.9
+            closeReviveOffer(); closeBlackMarket(); // v27.9: never inherit modals between runs
             state.surgeNextAt = state.runTime + 50;
             state.surgeEndsAt = 0; state.surgeLastSpawn = 0;
             // v26: biome rotation cadence
@@ -4324,6 +4401,52 @@
         // in-world). Drive over it to crack it open for a random reward.
         // ============================================
         let _supplyGroup = null; // { group, crate, beam, chute, x, z, groundY, bornAt, landed }
+            // v27.9 (owner): BLACK MARKET — a rare golden crate opens an in-run coin shop
+            const MARKET_ITEMS = [
+                { id: 'm_repair', icon: '🔧', name: 'Full Repair',   desc: 'Restore ALL health now',  base: 400, apply() { player.hp = player.maxHp; } },
+                { id: 'm_damage', icon: '💥', name: 'Damage Surge',  desc: '+20% damage for 60 seconds', base: 300, apply() { state.damageBoostUntil = Math.max(state.damageBoostUntil || 0, state.runTime + 60); } },
+                { id: 'm_shield', icon: '🛡️', name: 'Shield Charge', desc: 'Instantly charged shield',  base: 350, apply() { state.shieldUp = true; state.shieldReadyAt = 0; } },
+                { id: 'm_card',   icon: '🃏', name: 'Bonus Card',    desc: 'Instant upgrade card',      base: 800, apply() { state.pendingChoices = (state.pendingChoices || 0) + 1; } }
+            ];
+            function marketCost(m) { return Math.round(m.base * (1 + state.level / 25) / 10) * 10; } // scales gently with level
+            function openBlackMarket() {
+                state.gamePhase = 'paused'; // shop safely
+                const wrap = document.getElementById('market-items');
+                const render = () => {
+                    document.getElementById('market-coins').textContent = (state.coins || 0).toLocaleString();
+                    wrap.innerHTML = '';
+                    for (const m of MARKET_ITEMS) {
+                        const cost = marketCost(m);
+                        const row = document.createElement('button');
+                        row.className = 'hud-chip';
+                        row.style.cssText = 'display:flex; align-items:center; gap:10px; text-align:left; padding:10px 12px; width:100%; justify-content:flex-start; border-radius:12px;';
+                        row.innerHTML = '<div style="font-size:20px;">' + m.icon + '</div>' +
+                            '<div style="flex:1;"><div style="font-weight:700;">' + m.name + '</div>' +
+                            '<div style="font-size:11px; opacity:0.75; font-weight:400;">' + m.desc + '</div></div>' +
+                            '<div style="color:#ffd479; font-weight:700;">💰 ' + cost.toLocaleString() + '</div>';
+                        row.disabled = (state.coins || 0) < cost;
+                        if (row.disabled) row.style.opacity = '0.45';
+                        row.onclick = () => {
+                            if ((state.coins || 0) < marketCost(m)) return;
+                            state.coins -= marketCost(m);
+                            try { m.apply(); } catch (e) {}
+                            SFX.coin();
+                            render();
+                            updateHUD();
+                        };
+                        wrap.appendChild(row);
+                    }
+                };
+                render();
+                document.getElementById('market-dialog').classList.remove('hidden');
+                showBossBanner('🛒 BLACK MARKET — spend it while you have it');
+                SFX.achievement();
+            }
+            function closeBlackMarket() {
+                document.getElementById('market-dialog').classList.add('hidden');
+                if (state.gamePhase === 'paused') state.gamePhase = 'playing';
+                if ((state.pendingChoices || 0) > 0 && !state.isChoosingUpgrade) { state.pendingChoices--; showUpgradeChoices(); }
+            }
         const SUPPLY_REWARDS = [
             { w: 20, id: 'repair', label: '🔧 +40% Repairs', apply() { player.hp = Math.min(player.maxHp, player.hp + Math.ceil(player.maxHp * 0.4)); } },
             { w: 14, id: 'overcharge', label: '⚡ Overcharge 15s', apply() { state.overchargeUntil = Math.max(state.overchargeUntil || 0, state.runTime + 15); } },
@@ -4340,9 +4463,10 @@
                 const x = player.mesh.position.x + Math.cos(a) * d;
                 const z = player.mesh.position.z + Math.sin(a) * d;
                 const gy = getTerrainHeight(x, z);
+                const isMarket = state.level >= 6 && Math.random() < 0.18; // v27.9: rare golden BLACK MARKET crate
                 const group = new THREE.Group();
                 const crate = new THREE.Mesh(new THREE.BoxGeometry(1.6, 1.6, 1.6),
-                    new THREE.MeshStandardMaterial({ color: 0xb45309, emissive: 0xffd479, emissiveIntensity: 0.55, roughness: 0.5, metalness: 0.3 }));
+                    new THREE.MeshStandardMaterial({ color: isMarket ? 0xffd479 : 0xb45309, emissive: 0xffd479, emissiveIntensity: isMarket ? 0.9 : 0.55, roughness: 0.5, metalness: 0.3 }));
                 crate.castShadow = true;
                 group.add(crate);
                 const beam = new THREE.Mesh(new THREE.CylinderGeometry(0.7, 1.1, 34, 12, 1, true),
@@ -4355,8 +4479,8 @@
                 group.add(chute);
                 group.position.set(x, gy + 42, z);
                 scene.add(group);
-                _supplyGroup = { group, crate, beam, chute, x, z, groundY: gy, bornAt: state.runTime, landed: false };
-                showUpgradeNotification('📦 Supply drop incoming!');
+                _supplyGroup = { group, crate, beam, chute, x, z, groundY: gy, bornAt: state.runTime, landed: false, kind: isMarket ? 'market' : 'normal' };
+                showUpgradeNotification(isMarket ? '🛒 Black Market crate incoming!' : '📦 Supply drop incoming!');
                 tutorialTip('supply', '📦 <b>SUPPLY DROP</b> — drive to the gold beam and roll over the crate!', 6000);
             } catch (e) { _supplyGroup = null; }
         }
@@ -4367,6 +4491,13 @@
             _supplyGroup = null;
         }
         function collectSupplyDrop() {
+            if (_supplyGroup.kind === 'market') { // v27.9: opens the in-run shop instead of a random reward
+                const p = _supplyGroup.group.position.clone();
+                removeSupplyDrop();
+                createExplosion(p, 10, 0xffd479, 'armor');
+                openBlackMarket();
+                return;
+            }
             const total = SUPPLY_REWARDS.reduce((a, r) => a + r.w, 0);
             let roll = Math.random() * total, reward = SUPPLY_REWARDS[0];
             for (const r of SUPPLY_REWARDS) { roll -= r.w; if (roll <= 0) { reward = r; break; } }
@@ -5477,6 +5608,10 @@
             quitToMenu();
         });
 
+        document.getElementById('btn-revive-yes').addEventListener('click', (e) => { e.stopPropagation(); acceptRevive(); }); // v27.9
+        document.getElementById('btn-revive-no').addEventListener('click', (e) => { e.stopPropagation(); declineRevive(); }); // v27.9
+        document.getElementById('btn-market-leave').addEventListener('click', (e) => { e.stopPropagation(); closeBlackMarket(); }); // v27.9
+
         document.getElementById('btn-pause').addEventListener('click', (e) => {
             e.stopPropagation();
             togglePause();
